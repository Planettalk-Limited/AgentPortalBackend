<style>
        h1 {
            color: #059669;
            margin-bottom: 20px;
            font-size: 24px;
        }
        .success-icon {
            text-align: center;
            font-size: 60px;
            color: #059669;
            margin: 20px 0;
        }
        .payout-details {
            background-color: #f0f9f0;
            border: 2px solid #16a34a;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 8px 0;
            border-bottom: 1px solid #bbf7d0;
        }
        .detail-row:last-child {
            border-bottom: none;
        }
        .detail-label {
            font-weight: bold;
            color: #374151;
        }
        .detail-value {
            color: #1f2937;
        }
        .amount {
            font-size: 32px;
            font-weight: bold;
            color: #059669;
            text-align: center;
        }
        .status-badge {
            display: inline-block;
            background-color: #16a34a;
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 12px;
        }
        .success-box {
            background-color: #dcfce7;
            border-left: 4px solid #16a34a;
            padding: 15px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .info-box {
            background-color: #dbeafe;
            border-left: 4px solid #3b82f6;
            padding: 15px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .timeline {
            margin: 20px 0;
        }
        .timeline-item {
            margin-bottom: 15px;
            padding-left: 20px;
            position: relative;
        }
        .timeline-item.completed::before {
            content: '✅';
            position: absolute;
            left: 0;
            font-size: 16px;
        }
        .timeline-item.current::before {
            content: '🔄';
            position: absolute;
            left: 0;
            font-size: 16px;
        }
        .timeline-item.pending::before {
            content: '⏳';
            position: absolute;
            left: 0;
            font-size: 16px;
        }
        .button {
            display: inline-block;
            background-color: #00bcd4;
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: bold;
            text-align: center;
            margin: 20px 0;
        }
        .button.success {
            background-color: #16a34a;
        }
        .support {
            background-color: #f0f9ff;
            border: 1px solid #bae6fd;
            border-radius: 6px;
            padding: 15px;
            margin: 20px 0;
        }
        .celebrate {
            text-align: center;
            margin: 20px 0;
            font-size: 18px;
        }
    </style>

<div class="success-icon">🎉</div>
<h1 style="text-align: center;">Payout Approved!</h1>
        
        <div class="celebrate">
            <strong>Congratulations, {{firstName}}!</strong><br>
            Your payout request has been approved and will be processed soon.
        </div>

        <div class="payout-details">
            <div class="amount">${{amount}}</div>
            <div style="text-align: center; margin: 10px 0;">
                <span class="status-badge">Approved</span>
            </div>
            
            <div class="detail-row">
                <span class="detail-label">Payout ID:</span>
                <span class="detail-value">{{payoutId}}</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Agent Code:</span>
                <span class="detail-value">{{agentCode}}</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Payment Method:</span>
                <span class="detail-value">{{paymentMethod}}</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Approved Date:</span>
                <span class="detail-value">{{approvalDate}}</span>
            </div>
            {{#if netAmount}}
            <div class="detail-row">
                <span class="detail-label">Net Amount (after fees):</span>
                <span class="detail-value">${{netAmount}}</span>
            </div>
            {{/if}}
            {{#if estimatedArrival}}
            <div class="detail-row">
                <span class="detail-label">Estimated Arrival:</span>
                <span class="detail-value">{{estimatedArrival}}</span>
            </div>
            {{/if}}
            {{#if transactionId}}
            <div class="detail-row">
                <span class="detail-label">Transaction ID:</span>
                <span class="detail-value">{{transactionId}}</span>
            </div>
            {{/if}}
        </div>

        <div class="success-box">
            <strong>✅ Great news!</strong><br>
            Your payout has been approved by our finance team. The funds will be transferred to your designated {{paymentMethod}} account within {{processingTime}}.
            {{#if adminNotes}}
            <br><br><strong>Admin Notes:</strong> {{adminNotes}}
            {{/if}}
        </div>

        <div class="timeline">
            <h3>Payment Progress:</h3>
            <div class="timeline-item completed"><strong>Request Submitted</strong> - {{requestDate}}</div>
            <div class="timeline-item completed"><strong>Review Completed</strong> - {{approvalDate}}</div>
            <div class="timeline-item current"><strong>Processing Payment</strong> - Your funds are being transferred</div>
            <div class="timeline-item pending"><strong>Payment Complete</strong> - You'll receive confirmation once funds arrive</div>
        </div>

        {{#if paymentDetails}}
        <div class="info-box">
            <strong>Payment Destination:</strong><br>
            {{#if paymentDetails.bankAccount}}
            Bank Transfer to account ending in ****{{paymentDetails.bankAccount.accountNumber}}
            {{/if}}
            {{#if paymentDetails.paypal}}
            PayPal: {{paymentDetails.paypal.email}}
            {{/if}}
            {{#if paymentDetails.mobileMoney}}
            Mobile Money: {{paymentDetails.mobileMoney.provider}} - {{paymentDetails.mobileMoney.phoneNumber}}
            {{/if}}
            {{#if paymentDetails.airtimeTopup}}
            Airtime Credit: {{paymentDetails.airtimeTopup.phoneNumber}}
            {{/if}}
        </div>
        {{/if}}

        <a href="{{agentPortalUrl}}/payouts/{{payoutId}}" class="button success">View Payout Details</a>

        <div class="info-box">
            <strong>What's Next?</strong><br>
            • Your payment is now in our processing queue<br>
            • You'll receive another email when the payment is completed<br>
            • Funds typically arrive within {{processingTime}}<br>
            • Track your payout status in your agent dashboard
        </div>

        <div class="support">
            <strong>Questions About Your Payout?</strong><br>
            Our support team is available to help with any questions:
            <ul style="margin: 10px 0; padding-left: 20px;">
                <li>📧 Email: <a href="mailto:{{supportEmail}}">{{supportEmail}}</a></li>
                <li>📱 Phone: {{supportPhone}}</li>
                <li>💬 Live Chat: Available in your agent dashboard</li>
                <li>🎫 Reference ID: {{payoutId}} (please include in all communications)</li>
            </ul>
        </div>
