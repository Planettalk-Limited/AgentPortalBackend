<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - PlanetTalk</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 20px 0;
            background: linear-gradient(135deg, #e0f7fa 0%, #f0f4f8 100%);
        }
        .email-wrapper {
            max-width: 650px;
            margin: 0 auto;
            padding: 10px;
        }
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            border-radius: 12px;
            overflow: hidden;
        }
        .header {
            background: #ffffff;
            color: white;
            padding: 0;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 300;
        }
        .header .subtitle {
            display: none;
        }
        .content {
            padding: 30px 20px;
            background: #f8fafb;
        }
        .content h2 {
            color: #333;
            margin-top: 0;
            font-size: 24px;
        }
        .content h3 {
            color: #555;
            font-size: 18px;
            margin-top: 25px;
        }
        .content p {
            margin: 15px 0;
            font-size: 16px;
            line-height: 1.5;
        }
        .content ul {
            padding-left: 20px;
        }
        .content li {
            margin: 8px 0;
            font-size: 16px;
        }
        .button {
            display: inline-block;
            background: linear-gradient(135deg, #24B6C3 0%, #00acc1 100%);
            color: white !important;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 25px;
            margin: 20px 0;
            font-weight: 600;
            text-align: center;
            box-shadow: 0 4px 15px rgba(36,182,195,0.3);
            transition: all 0.3s ease;
        }
        .button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(36,182,195,0.4);
        }
        .button.success {
            background: linear-gradient(135deg, #28a745 0%, #1e7e34 100%);
            box-shadow: 0 4px 15px rgba(40,167,69,0.3);
        }
        .button.warning {
            background: linear-gradient(135deg, #ffc107 0%, #e0a800 100%);
            box-shadow: 0 4px 15px rgba(255,193,7,0.3);
            color: #212529 !important;
        }
        .button.danger {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
            box-shadow: 0 4px 15px rgba(220,53,69,0.3);
        }
        .info-box {
            background: #e9ecef;
            border-left: 4px solid #24B6C3;
            padding: 15px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .info-box.success {
            background: #d4edda;
            border-left-color: #28a745;
            color: #155724;
        }
        .info-box.warning {
            background: #fff3cd;
            border-left-color: #ffc107;
            color: #856404;
        }
        .info-box.danger {
            background: #f8d7da;
            border-left-color: #dc3545;
            color: #721c24;
        }
        .credentials-box {
            background: #f8f9fa;
            border: 2px solid #24B6C3;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            text-align: center;
        }
        .credentials-box h3 {
            margin-top: 0;
            color: #24B6C3;
        }
        .credential-item {
            margin: 10px 0;
            font-size: 16px;
        }
        .credential-value {
            font-weight: bold;
            font-family: 'Courier New', monospace;
            background: #e9ecef;
            padding: 5px 10px;
            border-radius: 4px;
            display: inline-block;
            margin-left: 10px;
        }
        .footer {
            background: #343a40;
            color: #ffffff;
            padding: 20px;
            text-align: center;
            font-size: 14px;
        }
        .footer p {
            margin: 5px 0;
        }
        .footer a {
            color: #24B6C3;
            text-decoration: none;
        }
        .social-links {
            margin: 15px 0;
        }
        .social-links a {
            display: inline-block;
            margin: 0 10px;
            color: #6c757d;
            text-decoration: none;
        }
        @media (max-width: 600px) {
            body {
                padding: 10px 0;
            }
            .email-wrapper {
                padding: 5px;
            }
            .email-container {
                width: 100% !important;
                margin: 0 !important;
                border-radius: 8px;
            }
            .content, .footer {
                padding: 20px 15px !important;
            }
            .button {
                display: block;
                text-align: center;
                margin: 20px 0;
            }
        }
    </style>
</head>
<body>
    {{#if previewText}}
    <div style="display: none; max-height: 0px; overflow: hidden;">
        {{previewText}}
    </div>
    <div style="display: none; max-height: 0px; overflow: hidden;">
        &nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;&zwnj;&nbsp;
    </div>
    {{/if}}
    <div class="email-wrapper">
        <div class="email-container">
            {{> header}}
            
            <div class="content">
                {{{body}}}
            </div>
            
            {{> footer}}
        </div>
    </div>
</body>
</html>
