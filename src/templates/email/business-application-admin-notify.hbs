<p><strong>New business partner self-registration</strong></p>

<p>Submitted: {{submittedAt}}</p>

<ul style="list-style: none; padding-left: 0;">
  {{#each detailLines}}
  <li style="margin: 4px 0;">{{this}}</li>
  {{/each}}
</ul>

<p style="margin-top: 20px;">Review pending users in the admin area (status: <code>awaiting_partner_approval</code> after email verification), then approve with a custom partner code.</p>

<p style="margin-top: 12px; font-size: 14px; color: #444;"><strong>Process:</strong> review the application details above, then approve with a custom partner code or reject with an optional reason.</p>
